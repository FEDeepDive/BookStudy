## 아이템 28. 유효한 상태만 표현하는 타입을 지향하기
- 이해를 돕기 위해 든 예시가 그리 효과적이지는 않은 것 같네요
- 저는 작업할 때 특정 작업의 성공과 실패를 명시적으로 다루기 위해 아래와 같은 유니온으로 이루어진 결과 타입을 많이 사용합니다
```ts
type Success<T> {
  success: true; // boolean이 아님에 주의
  value: T;
}

type Failure<E> {
  success: false; // boolean이 아님에 주의
  error: E;
}

type Result<T, E> = Success<T> | Failure<T>;

// example
const someProcess: Result<number, Error> = {
    // ...
}

const useSomeProcess = (result: Result<number, Error>) => {
  if (result.success) {
    // do something with result.value
    // cannot access to result.error
    console.log(result.value)

    return;
  }

  // do something with result.error
  // cannot access to result.value;
  console.error(result.error);
}

useSomeProcess(someProcess());
```
- 이게 좀 번거로울 때도 있는데 익숙해지면 예외 상황을 다루지 않아도 되어서 결과를 예측하기 쉬워지더라구요

## 아이템 29. 사용할 때는 너그럽게, 생성할 때는 엄격하게
- 책에서 사용한 바와 비슷하게 위경도를 다루는 실전적인 라이브러리로는 네이버 지도를 들 수 있겠습니다 ([#](https://navermaps.github.io/maps.js.ncp/docs/global.html))
  - `Bounds`, `Coord` 등을 보시면 위경도 정보 `LatLng`와 함께 x/y 좌표를 의미하는 `Point`를 보실 수 있지용
- 느슨한 타입은 "기본값"을 지정할 수 있는 대부분의 라이브러리에서 사용하는 일종의 패턴으로도 볼 수 있겠습니다
  - ex. [react-hook-form](https://github.com/react-hook-form/react-hook-form/blob/3f4d24ce0734a5f573a77b701822b68d4c242203/src/types/form.ts#L35)

## 아이템 30. 문서에 타입 정보를 쓰지 않기
- 주석에는 해당 코드에 담긴 작성자의 '의도'를 설명하는 것이 좋다는 이야기를 들었던 적이 있네요
  - 예를 들어 특정 제약 조건 때문에 코드 구현체가 너무 복잡해진 경우 왜 이렇게 할 수밖에 없었는가에 대해 설명한다던가...
- 요약 두 번째는 굉장히 의미가 있어보이는데, 프로그램에서 시간을 숫자로 다루는 경우 대부분 밀리세컨드를 사용한다는 것은 보통 경험적으로(다시 말해, 오류에 얻어맞아보고) 알게 되더라구요
  - 특히 자바스크립트와 같이 '정수'와 '실수'를 동일한 타입으로 다루는 언어로 작업하는 경우, 백분율 등을 사용하는 코드를 만지게 된다면 이런 '문맥'을 추가해주는 게 좋다는 의미로 보입니다

## 아이템 31. 타입 주변에 null 값 배치하기
- 아이템 28과 어느 정도 이어지는 부분이 있다고 봅니다
- 저는 같이 작업하는 팀원 분들과 논의하여 "외부에서 들어오는 값에만 `null`을 허용하고 우리 코드 내부에서 해당 데이터를 다룰 때는 최대한 유니온 타입, 최악의 경우 `undefined`로 다루자"고 합의한 바가 있습니다
  - 협업하는 인원끼리 이런 식으로 규칙을 정하는 게 좋을 것 같아요

## 아이템 32. 유니온의 인터페이스보다는 인터페이스의 유니온을 사용하기
- 내 사랑 tagged union... 만들 때나 다룰 때나 다소 번잡하다 느껴질 때도 있지만, '명시적인 것이 암시적인 것보다 낫다'는 말도 있지 않겠습니까
- `in` 연산자는 써본 적이 없는데... 머릿속에 잘 넣어뒀다가 잘 써먹겠습니다