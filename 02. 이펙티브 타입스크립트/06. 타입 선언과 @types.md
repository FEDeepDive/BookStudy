## 감상평

### `보경`

### `미림`

- TSDoc 주석이 마크다운 형식으로 꾸며진다는 것, 객체 필드에 TSDoc으로 주석을 달면 필드별로 설명을 볼 수 있다는 것을 처음 알았다.
- `dtslint` 되게 편해보인다 주석으로만 적어주면 되니까
  - '제대로 된 타입 언어라면 애초에 타입을 테스트할 필요가 없을 것'이라는 정욱님 말씀에 '그렇네?' 싶었다. C 언어의 타입을 누가 테스트 하겠는가? 좋게 말하면 유연성, 나쁘게 말하면 허점이라고 표현할 수도 있겠다.
- 일반적인 타입스크립트 사용보다는 특수한 상황(ex. 라이브러리 제작 등)에서 겪는 일들을 이야기하기 시작하는 걸 보니, 이 책도 거의 다 읽었구나 가 실감됨.. 앞으로 두 장 남았다..!

### `정욱`
### 아이템 45. devDependencies에 typescript와 @types 추가하기
- 저는 `DefinitelyTyped`가 처음에 이미 잘 돌아가는 라이브러리 메인테이너들이 타입까지 신경쓸 여유가 없을 때 기여받는 그런 건줄 알았어요
- 실제로 자기는 유지보수할 시간 없다고 하고 싶으면 거기다 하라는 메인테이너 댓글도 본 적 있네요

### 아이템 46. 타입 선언과 관련된 세 가지 버전 이해하기
- 요즘엔 대부분의 라이브러리들이 다들 자체 선언을 들고있는 게 일반적인 것 같습니다
- 그냥 자바스크립트로 작성되는 일정 규모 이상의 라이브러리가 없어서 그런가 싶기도 하구요...

### 아이템 47. 공개 API에 등장하는 모든 타입을 익스포트하기
- 만약 타입스크립트가 명목적 타입 시스템을 사용했다면 애초에 이럴 일도 없었을 듯 하네요
  - 같은 구조를 가지고 있어도 호환이 안되니까 그냥 못 쓰는 함수가 되어버리는 거죠

### 아이템 48. API 주석에 TSDoc 사용하기
- 저는 그래서 대부분의 경우 함수 밖에다 주석을 쓸때는 `/** */` 형식으로, 안에다가 쓸때는 `//`으로 쓰고 있어요

### 아이템 49. 콜백에서 this에 대한 타입 제공하기
- 요즘 리액트 쓰면서 `this`를 써본 일이 거의 없어서 `this`에 타입을 잡아본 일도 없는 것 같아용
- 가능한 곳에는 모두 화살표 함수를 바르고 진짜 필요한(타입이 걸리는... ex. 247p) 곳에만 함수 표현식을 쓰고 있는데 대부분의 경우에는 다 대체가 되다보니...

### 아이템 50. 오버로딩 타입보다는 조건부 타입을 사용하기
- 조건부 타입이랑 제네릭 잘 쓰면 뭔가 타입스크립트 고수같아보일듯...

### 아이템 51. 의존성 분리를 위해 미러 타입 사용하기
- 라이브러리를 작성할 일이 생기면 유의할 점이네요

### 아이템 52. 테스팅 타입의 함정에 주의하기
- 사실 이 부분은 타입스크립트의 타입 시스템이 그리 ['안전(건전?)'하지 못하다](https://www.typescriptlang.org/ko/docs/handbook/type-compatibility.html#%EA%B1%B4%EC%A0%84%EC%84%B1%EC%97%90-%EB%8C%80%ED%95%9C-%EC%B0%B8%EA%B3%A0%EC%82%AC%ED%95%AD-a-note-on-soundness)는 사실을 보여줍니다 ([#](https://kohandy.tistory.com/18))
- 이 정도에 만족하지 못하는 사람들은 더욱 안전(하고 자유롭지 못)한 세상을 찾아 떠나게 되죠...

### `영은`
