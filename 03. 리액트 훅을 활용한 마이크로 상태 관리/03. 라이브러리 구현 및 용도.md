## 감상평

### `보경`

### `미림`

6-7장
- p138 세 번째 문단 오타: `TodoItem` 컴포넌트를 만들어 보겠다 → `TodoList` ...
- 6장... 이게 뭘까 내가 이해를 못한건지 아님 내용이 그냥 그런 건지? 읽었는데 선택자 함수랑 상태 사용 추적 두 개 빼고는 머리에 남는 게 없는 것 같다.. 그래서 뭐 어쩌라는거지? 싶고.. 본격적인 라이브러리 비교분석 들어가기 전에 정리하기 위한 장 같은데 좀 흐릿흐릿하게(?) 정리한 것 같음.
- ‘전역 상태 관리 라이브러리의 리렌더링 최적화 방법을 알면 적합한 전역 상태 관리 라이브러리를 선택할 수 있다’고 하는데, 그것도 맞는 말이지만 나한테는 그보다 규모, 복잡성, 사용성, 호환성, 커뮤니티 등등이 라이브러리 선택에 더 중요하게 작용했어서 크으게 공감되진 않는거같음
- 공식문서를 읽는 것과 다르게 리렌더링 관점이나 어떤 식으로 사용하면 어떻게 된다든가 하는 사용 사례를 조금 더 풀어서 설명하는 느낌이라 좋다. 공식 문서는 도구 자체를 설명한다면 이 책은 전역 상태 관리 라이브러리로서 이 도구가 어떤 특징을 갖는지, 리액트에서는 어떻게 쓰는지 등을 알려주고 있어 더 콤팩트하고 쉽게 이해할 수 있는 것 같다.

8장
- p157 `Counter` 정의에서 props가 정의되어 있는데 p158에서 사용할 때에는 props를 전달하지 않고 있음.. 다른 절에서 정의한 거랑 같다는데 같지도 않고.. 깃허브 예제코드 보면 거기엔 props으로 받지 않고 그냥 바로 `countAtom`을 사용하고 있음. 오류인 듯
- 컨텍스트와 Jotai의 차이점을 이야기하면서 “동적 아톰 생성”이 가능하다는 점을 꼽고 있는데, 이 부분이 이해가 잘 안된다…
- 파생 아톰을 필요할 때마다 만들어야 하는 건가? 만약 그렇다면 좀 번거로울 것 같음. zustand는 필요한 것만 쏙쏙 골라 쓰는 graphQL같은 느낌이고, jotai는 백엔드가 일일이 프론트엔드 맞춤형 API를 만들어주는 느낌인데(이런 걸 ViewModel 이라고 하나 봄) 요즘 후자가 힘들어서 graphQL에 관심이 생기고 있는지라 살짝 불편할 것 같다는 생각이 들었다. + zustand가 이런 사용성 면에서 편리해서 인기가 있나보다 하는 생각이 듦
    - ⇒ 그게 아니라고 하네요~ 파생 아톰은 컴포넌트마다 맞춤형으로 만든다 << 라고 이해했는데 그게 아니라 도메인에서 의미 있는 파생 값, 계산된 값을 만들어야 할 때 일반적으로 만든다고 함
- 아톰 모델을 잘 이해하고 사용한다면 정말 좋을 것 같다. 단순히 사용하기만 하는 게 아니라 아톰 모델과 Jotai에서 제공하는 기술들을 활용할 줄 안다면 확실히 고점을 뽑을 수 있을 것 같다(todolist 예제처럼). 저점은 평타고 고점이 높다는 생각이 듦. 그만큼 익숙해지는 데 시간이 걸리고 이해하고 활용하기가 어려운 것 같다. 내가 잘 알고 잘 써도 같은 팀원이 이해를 잘 못하면 어려워할 것 같다…물론 이건 다른 기술도 다 그렇겠지만

9장
- 오.. 되게 간단하다 전역 상태 관리하는 것 같지 않다고 생각할 정도로 간단함
- 근데 쓸 때 헷갈릴 거 같다 리액트는 기본적으로 불변인디 valtio는 가변 갱신 모델이니까
- JS 함수 쓸 수 있는 건 너무 너무 너무 너무 좋은 거 같다!!!!
- 은총알은 없으니 목적에 맞게, 취향에 맞게 선택해서 행복 개발을 하자

10장
- 리액트 컨텍스트의 리렌더링 문제를 해결하기 위한 라이브러리가 이렇게나 많다. 방식을 정리하자면…
    - use-context-selector: 일부 필드만 선택해 구독할 수 있게 함
    - React Tracked: 프록시 기반으로 사용 상태 추적 (내부적으로 use-context-selector 사용)
- 전역 상태 관리 라이브러리는 아니지만, 컨텍스트를 사용해 전역적으로 어떤 데이터를 다뤄야 할 때 렌더링 최적화를 위해 사용할 수 있고, 또 전역 상태 관리 라이브러리의 선택자 함수를 향상해 전역 상태를 더 잘 다룰 수 있다는 것을 소개하고자 내용을 담은 듯 하다. 굿굿
- 7장 Zustand 스터디 때 선택자 함수로 필요한 상태들을 하나씩 불러오는 방식이 불편하다는 이야기를 했던 것 같은데, React Tracked로 이런 점을 보완할 수 있을 것 같다(예시 코드처럼). 이럴 때마다 문제를 해결하는 도구(방식)는 많은데 그 존재를 알지 못해서 문제를 떠안고 사는 경우가 종종 있다고 느낀다. 어쩔 수 없는 거지만, 생태계에 관심을 갖고 꾸준히 소식을 접하려고 하다보면 지금처럼 하나씩 얻어갈 때가 있는 것 같다.
- 새삼스럽게 자바스크립트의 프록시 객체를 플러그인같은 게 아니라 언어 자체에서 제공해준다는 점에서 신기함? 감사함? 황당? 감탄스러움? 딱 매치되는 감정이 없는데 아무튼 새삼 새롭게 느낀다 ㅋㅋ

### `정욱`
6장
- 전역 상태 관리는 결국 읽기와 쓰기를 어떻게 효율적으로 잘 동작하게 하는지가 중요!
- 전역 변수를 직접 변경하는 것보다 변경하는 함수를 제공해야 한다는 점은 객체지향 같은 데서 멤버 변수를 직접 노출하지 말라는 말도 생각난다!
  - `setter`를 지양하라는 표현도 넓게 보자면 아마 엮이는 부분이 있을 듯?
- 데이터 중심 접근 방식은 나름 익숙한데, 컴포넌트 중심 접근 방식은 뭔가 자주 접한 느낌은 아닌 것 같기도?
- 컴포넌트 생명 주기 내에서 전역 상태를 유지하기? 적절한 예시를 들어주면 좋겠다
  - 컴포넌트 상태는 주로 컴포넌트 중심 접근 방식에서 사용된다 -> 컴포넌트 상태는 대체로 `useState`(또는 `useRef`) 훅으로 구현된다
    - 이건 그냥 우리가 너무 익숙하게 접하는 지역 상태 방식으로 보이는데? 그냥 위에서 컨텍스트로 내려주면 다 전역 상태로 봐주는건감
- 선택자와 메모이제이션 파트는 늘 공부했던 참조 타입은 그 내부 값이 같아도 참조가 다르면 다른 값이라는 이야기의 연장선상으로 보임
- 속성 접근 감지... 책을 쓰시던 그 시절에는 이렇게 단순히 리액트 리렌더링 최적화의 한 갈래로만 생각하셨겠지만, 2025년 현재에는 뭐, 리액트 [외에](https://ko.vuejs.org/guide/essentials/reactivity-fundamentals#reactive-proxy-vs-original-1)[서도](https://svelte.dev/docs/svelte/$state#Deep-state) 빈번하게 채택되는 개념이 아닐런지
- 개인적으로는 아톰 방식이 제일 자유도가 높은 것 같긴한데 그만큼 난이도가 높은 것 같다
  - bottom-up 방식이 top-down보다 초기 난이도는 높지만 유지보수에 유리한 것처럼?

7장
- 데이터 중심 접근을 택한 라이브러리 중 하나로 이해하면 되겠지
- 불변 상태 모델의 장점 - 상태 객체의 참조에 대한 동등성만 확인하면 변경 여부를 알 수 있다?
  - 값을 바꿀 거면 무조~건 새 객체를 집어넣어야 하고 안쪽 프로퍼티를 바꾸는 경우는 없기 때문에 객체가 다르면 그냥 다른 거!!!
- `create` 함수는 그러면 일종의 커스텀 훅 팩토리?인가?
  - 함께 읽으면 괜찮을 듯한 [글](https://yunicornlab.tistory.com/103)도 발견
- 투두리스트는 아주 유명한 기술 데모 아이템임
- 개인적으로는 Zustand를 쓸 때 선택자 함수를 기술한 적은 없고, 불변 상태 모델을 기반으로 한다는 점은 참 마음에 들었음
  - 리액트의 근본과 참 잘 어우러지는 라이브러리이긴 한데... 그 아이디어에서 벗어나는 상태 관리 방법이 필요한 경우에는 좀 다른 도구를 선택할 필요가 있겠다! 좀 인사이트가 생긴 느낌

8장
- 계속 보면서 드는 생각이, 이해를 돕기 위해서라는 이유로 몇번이고 같은 코드를 보여주는 경향이 있는 것 같은데 좀 불필요한 것 같음
  - 카운터 + 이를 컨텍스트로 해결하는 예제라던지, `text` 관련한 거라던지, Jotai를 설명하다가 갑자기 이전 방식으로 돌아오니 좀 헷갈릴 것 같고... 이런 거 굳이 또 넣었어야 했나
  - 154p에서도 Jotai 이야기 잘 하다가 또 갑자기 선택자 접근 방식 살펴보고...
- Jotai를 쓰면 `Provider`를 쓸 일이 없을 거라고 예전엔 막연하게 생각했었는데 꼭 그렇지만도 않더라...
  - 아무래도 '전역' 상태다보니 어느 페이지를 가든 값이 살아있었는데, 특정 페이지에 접근할 때마다 초기화해줘야 하는 경우 그냥 트리 상단에 `Provider`를 씌우면 간단했음
- 공식 문서에 항목 자체가 있긴 한 [Atoms-in-atom](https://jotai.org/docs/guides/atoms-in-atom)은 처음 봤을 땐 신기했는데 보다보니 왜 이 생각을 못했지 하는 생각이 듦...
  - [`toString()`](https://github.com/pmndrs/jotai/blob/main/src/vanilla/atom.ts#L108)
- [유용해보이는 고급 기능](https://jotai.org/docs/extensions/query)

9장
- 원래 변수를 선언해두고 그 변수를 변경한다고 해서, 그 변수를 사용하는 다른 변수의 값이 그에 따라 변경되는 일은 없다!
  - 그러나? Proxy 객체는 속성을 읽어오는 로직과 변경하는 로직을 개발자 마음대로 재정의할 수 있기 때문에 적절히 구현해주면 변경을 감지할 수 있게도 만들어진다!
  - 개별 속성에 대한 변경을 감지할 수 있기 때문에 사용하는 코드를 봤을 때 직관적으로 아! "여기서" 값을 바꾸는구나!를 알 수 있음
    - (문제는 결국 온갖 곳에서 바꾸기 시작하면 정작 필요한 부분을 찾기 힘들어진다는 점도...)
    - 그렇게 생각하면 저는 결국 Vue나 Svelte를 사랑할 수는 없겠다는 생각도 드네요
- 용법만 보면 약간 [Immer](https://immerjs.github.io/immer/)가 생각나는 듯한데
  - 다만 Immer는 "상태"를 관리하는 라이브러리가 아니고, 문법만 그렇지 사실상 내부 구현적으로는 불변 상태 모델을 사용하지만
  - Proxy 구현체를 통해 실제로 그 객체의 값이 변경되는 Valtio와 달리, Immer는 가변 상태 변경 코드에 대응하는 불변 상태 갱신 로직이 실행됨 
- 객체에 대해 strict equality 연상자를 쓸 수 있는 것은 유용한 경우가 있겠지만, 이미 불변 상태 모델에 뇌가 절여진 내가 이런 코드에 익숙해질 수 있을까...?
- 변경 가능한 갱신이 아마 성능적으로는 분명 약간 더 뛰어날 것이나... 리액트의 근본 사상이 불변 상태를 사랑하는 이상 대중화되기는 힘들 듯
  - 바꿔 말하면 변경 가능한 갱신이 일반적인 환경에서는 충분히 제 역할을 할 수 있겠다는 생각이 듦
  - React를 안 붙이고 게임을 만든다던지?
  - 그러나 이 사람들 또 참지 못하고 그에 대한 새로운 상태관리 [라이브러리](https://github.com/pmndrs/koota)를 만들었답니다
- "필요에 맞는 해결책"을 찾는 것도 중요하지만, 목표가 그리 특이하지 않을수록 본인의 사고방식과 잘 맞는 도구를 잘 고르는 것도 중요하겠다

10장
- 상태 관리라기보다는 이미 작업이 좀 많이 진행된 코드베이스의 성능을 최적화하는 데에 쓰이는 라이브러리?
- 컨텍스트의 내부 구현체만 프락시로 샥 갈아 끼우는 듯한 느낌
- 개인적으로 중요하다고 느끼는 점은 상태를 자동으로 관리하겠다고 정의하는 부분은 `useContext`의 문법을, 실제로 갖다쓰는 용법은 `useState`나 `useReducer`의 문법을 닮아있다는 것!
  - 추가로 문법이나 구조를 학습할 필요 없이 리액트 개발자에게 자연스러운 형태로 제공한다는 점이 약간 마음에 듦
  - 아무리 성능이 좋다고 해도 쓰기 불편하면 굳이?라고 할 정도로 DX의 중요성이 늘어난 시대다보니
- `Redux`를 앞으로 쓸 일이 있을까 싶어서 꼭 봐야 하나 싶지만, 211p의 `useSelector`와 `useTrackedState`를 사용할 때 개발자에게 느껴지는 차이는 좀 의미가 있을 듯
  - 하나부터 열까지 다 내손내짠 코드를 좋아하는 사람이라면 `useSelector` 방식이 마음에 들 것 같고 그 반대는 `useTrackedState`를 좋아하지 않을까?
  - 그런데 이것도 결국 AI 시대가 되면서 진짜 내손내짠이 얼마나 의미를 가지려나 싶기도 하고...
- 마지막 부분에서 `useContextSelector` RFC를 다루는 PR을 소개하는데 머지 여부가 불투명하다는 게 참 개발바닥이 빠르긴 하다는 생각이 듦
  - AI 뭐 그런 수준의 이야기가 아니고 결국 리액트 컴파일러 시대가 시작되었기 때문...
  - 리액트 상태 관리와 리렌더링 최적화에 많은 시간을 할애했을 저자가 좀 안타깝게 느껴지기도 함
    - [본인은 그래도 의미가 사라지지는 않을거라고 말하긴 했지만...](https://blog.axlight.com/posts/thoughts-on-state-management-libraries-in-the-react-compiler-era/)

11장
- 책에서 말한 '기능 디렉토리' 패턴이 뭔지 궁금했음... 역자주에 [이런 거](https://redux.js.org/faq/code-structure) 달아줬으면 좋았겠는데
- Immer 기본 적용이라는 게 신기하면서도 굳이 싶음... 대체적으로 Redux는 사용자를 하나의 옳은 방향으로 끌고가고 싶어하는 모습을 보이는듯
- [Zustand와 Context 같이 쓰기](https://velog.io/@ojj1123/zustand-and-react-context)도 사실 이제는 속세에 많이 알려진 내용이라
- 근데 사실 요즘 Recoil 쓰는 사람이 있나요?
- 마이크로 상태 관리 도구는 요구사항에 따라 '상태가 위치해야 하는 곳'과, 개발자의 멘탈 모델에 따른 '갱신 방식'을 고려하여 결정해야 한다!
- 저번 느낀 점에다 리액트 컴파일러 세상이 오게 되면서 저자가 안타깝다는 이야기를 했던 것 같은데, 저자가 작성한 글 내용 중 "전역 상태 관리 라이브러리는 상태를 구조화하고 조합하며 외부 서비스에 연결하는 방법을 제공한다"는 말이 다시 떠오르며 느낀 바가 생겼음
  - 실질적인 성능 문제를 해결하기 위해 나왔다고는 하지만 결국 상태를 잘 관리(ㅋㅋ)하는 방법론에 대한 여러 예시를 들어준 거라고 봐야할 듯
  - 더 나아가서 사용자들에게 상태 관리 도구를 선택할 때 고려해야 할 기준을 정해줬다는 큰 가치가 남아있긴 하다고 생각
    - 왜냐? 성능이 해결되면 그 이후부터는 또 가독성과 개발자 경험을 따지게 되기 때문!
- 원래 상태 관리 도구를 선택할 때 진짜 이유없이 손 가는 대로 선택했던 기억이 나는데, 좀 더 의미있는 선택을 할 수 있게 된 것 같아 남는 게 있는 것 같음
  - 내가 내린 결정에 이유를 말할 수 있는 사람이 되고 싶으니까...

### `영은`
